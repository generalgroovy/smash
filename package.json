{
  "name": "smash-movement-lab",
  "version": "0.4.0",
  "private": true,
  "description": "A dependency-free, deterministic platform-fighter movement lab.",
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "check": "node --check engine.js && node --check animation.js && node --check visual.js && node --check game.js && node --check serve.cjs && node --check scripts/build.cjs",
    "serve": "node serve.cjs",
    "build": "node scripts/build.cjs",
    "verify": "npm run check && npm test && npm run build"
  },
  "engines": {
    "node": ">=18"
  }
}
